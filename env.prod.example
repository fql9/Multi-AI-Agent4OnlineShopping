# ============================================================
# Multi-AI-Agent Shopping System - PRODUCTION Configuration
# ============================================================
# 生产环境配置示例
# 复制此文件为 .env 并填写实际值
# ============================================================

# ----------------------------------------
# 🌍 Environment Mode
# ----------------------------------------
APP_ENV=production

# ----------------------------------------
# 🗄️ PostgreSQL Database
# ----------------------------------------
POSTGRES_USER=agent
POSTGRES_PASSWORD=your_secure_password_here
POSTGRES_DB=agent_db
POSTGRES_PORT=25432

# ----------------------------------------
# 🔴 Redis Cache
# ----------------------------------------
REDIS_PASSWORD=your_secure_redis_password_here
REDIS_PORT=26379

# ----------------------------------------
# 🔧 Service Ports (External / Host Machine)
# ----------------------------------------
TOOL_GATEWAY_PORT=28000
CORE_MCP_PORT=28001
CHECKOUT_MCP_PORT=28002
AGENT_PORT=28003
WEB_APP_PORT=28004

# ----------------------------------------
# 🔑 OpenAI API Configuration (Required)
# ----------------------------------------
OPENAI_API_KEY=sk-your-production-api-key
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL_PLANNER=gpt-4o
OPENAI_MODEL_VERIFIER=gpt-4o

# ----------------------------------------
# 💰 Token Budget
# ----------------------------------------
TOKEN_BUDGET_TOTAL=50000
TOKEN_BUDGET_PER_STEP=5000

# ----------------------------------------
# 📦 XOOBAY API (Product Data Source)
# ----------------------------------------
# ⚠️ 生产环境强烈建议开启，否则 DB 数据不足时容易"搜不到商品"
XOOBAY_ENABLED=true
XOOBAY_API_KEY=your_xoobay_api_key
XOOBAY_BASE_URL=https://www.xoobay.com
XOOBAY_LANG=en

# ----------------------------------------
# 🌐 CORS Configuration
# ----------------------------------------
# 生产环境使用实际域名
CORS_ORIGINS=https://yourdomain.com

# ----------------------------------------
# 🚦 Rate Limiting (Tool Gateway)
# ----------------------------------------
# ⚠️ 生产环境建议开启限流保护服务
# ⚠️ 注意：阈值需要足够高，覆盖监控 + LB 健康检查 + 正常用户请求峰值
# 
# 如果遇到 /health 被 429，有两个选择：
# 1. 提高 RATE_LIMIT_MAX（推荐先尝试）
# 2. 修改 tool-gateway 代码给 /health 加白名单
#
RATE_LIMIT_ENABLED=true
RATE_LIMIT_MAX=1000
RATE_LIMIT_WINDOW=60000

# ----------------------------------------
# 🔄 Session Configuration
# ----------------------------------------
SESSION_TTL_HOURS=24

# ----------------------------------------
# 🎛️ Feature Flags
# ----------------------------------------
MOCK_TOOLS=false
ENABLE_RAG=true

# ----------------------------------------
# 📊 Logging
# ----------------------------------------
# 生产环境建议 info 或 warn
LOG_LEVEL=info

# ----------------------------------------
# 🌐 Frontend Configuration
# ----------------------------------------
# 生产环境使用实际域名或服务器 IP
NEXT_PUBLIC_TOOL_GATEWAY_URL=https://api.yourdomain.com
NEXT_PUBLIC_AGENT_API_URL=https://agent.yourdomain.com
