# 环境变量配置示例 (简化版)
# 复制此文件为 .env 并填入真实值
# For full configuration, see .env.example

# ========================================
# LLM 配置（必需）
# ========================================
# 推荐使用 GPT-4o，mini 模型在处理复杂 Schema 时容易出错
OPENAI_API_KEY=sk-your-api-key-here
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL_PLANNER=gpt-4o
OPENAI_MODEL_VERIFIER=gpt-4o

# ========================================
# Token 预算
# ========================================
TOKEN_BUDGET_TOTAL=50000

# ========================================
# 数据库配置（Docker 内部使用，通常不需要修改）
# ========================================
POSTGRES_USER=agent
POSTGRES_PASSWORD=agent_dev_password
POSTGRES_DB=agent_db

# ========================================
# XOOBAY API 配置（可选，用于产品数据源）
# ========================================
XOOBAY_ENABLED=true
XOOBAY_API_KEY=xoobay_api_ai_geo
XOOBAY_BASE_URL=https://www.xoobay.com
XOOBAY_LANG=zh_cn

# ========================================
# 服务端口（外部访问）
# ========================================
# 这些是暴露在主机上的端口
WEB_APP_PORT=28004
AGENT_PORT=28003
TOOL_GATEWAY_PORT=28000
